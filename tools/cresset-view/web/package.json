{
  "name": "cresset-view-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite",
    "test": "for suite in graph anchor threads; do esbuild \"src/$suite.test.ts\" --bundle --platform=node --format=esm --outfile=\".test-$suite.mjs\" --log-level=warning && node \".test-$suite.mjs\" || exit 1; done"
  },
  "dependencies": {
    "@fontsource/archivo": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "@pierre/diffs": "1.2.12",
    "@pierre/trees": "1.0.0-beta.5"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "7.3.6"
  }
}
